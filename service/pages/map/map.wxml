<view class="top-bar">
  <image src="https://636c-cloud1-6gsqyvkd3f24bdd8-1311119192.tcb.qcloud.la/images/logo.png" class="logo"></image>
  <text class="top-title">校友地图</text>
</view>

<map
  id="map"
  longitude="{{longitude}}"
  latitude="{{latitude}}"
  scale="12"
  markers="{{markers}}"
  show-location
  enable-poi="false"
  class="map"
  bindmarkertap="onMarkerTap">
</map>

<view class="unit-list">
  <view class="title">📍附近校友单位</view>

  <t-cell-group>
    <t-cell
      wx:for="{{nearestUnits}}"
      wx:key="id"
      title="{{item.company}}"
      arrow
      hover
      bind:click="onUnitTap"
      data-id="{{item.id}}"
    />
  </t-cell-group>
</view>


